version: '3.8'

services:
  client:
    image: node:12-alpine
    environment:
      NODE_ENV: development
    ports:
      - 8080:8080
    volumes:
      - ./services/client:/app
    working_dir: /app